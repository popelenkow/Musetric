{
	"name": "musetric-api",
	"description": "Musetric is a javascript rest api library for building music applications",
	"version": "0.0.4",
	"license": "MIT",
	"main": "index.js",
	"types": "index.d.ts",
	"homepage": "https://github.com/popelenkow/Musetric",
	"bugs": "https://github.com/popelenkow/Musetric/issues",
	"repository": {
		"type": "git",
		"url": "https://github.com/popelenkow/Musetric",
		"directory": "musetric"
	},
	"keywords": [
		"musetric-api"
	],
	"engines": {
		"node": ">=16.0.0"
	},
	"scripts": {
		"build": "rimraf dist && node scripts/build.js && tsc",
		"dev": "rimraf dist && node scripts/build.js && tsc --watch",
		"lint": "eslint **/*.{js,ts,tsx}",
		"lint:fix": "eslint **/*.{js,ts,tsx} --fix",
		"lint:config": "rimraf .eslintrc.local.json && eslint --print-config .eslintrc.js >> .eslintrc.local.json"
	},
	"resolutions": {
		"glob-parent": "^6.0.0"
	},
	"dependencies": {},
	"devDependencies": {
		"copyfiles": "^2.4.1",
		"eslint-plugin-musetric": "0.0.2",
		"typescript": "5.1.6"
	}
}
