import { RgbGradient } from '../common';

export type Colors = {
  played: string;
  unplayed: string;
  background: string;
};

export type Gradients = {
  played: RgbGradient;
  unplayed: RgbGradient;
};
