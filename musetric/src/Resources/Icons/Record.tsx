import React, { FC } from 'react';
import { AiFillAudio } from 'react-icons/ai';

export type RecordIconProps = {
};

export const RecordIcon: FC<RecordIconProps> = () => {
	return (
		<AiFillAudio size='24px' />
	);
};
