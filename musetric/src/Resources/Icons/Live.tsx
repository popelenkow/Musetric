import React, { FC } from 'react';
import { CgMediaLive } from 'react-icons/cg';

export type LiveIconProps = {
};

export const LiveIcon: FC<LiveIconProps> = () => {
	return (
		<CgMediaLive size='26px' />
	);
};
